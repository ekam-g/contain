import { Button, VerticalBox } from "std-widgets.slint";

export component MyApp inherits Window {
    preferred-width: 600px;
    preferred-height: 600px;
    title: "Home - Contain";
    in-out property <string> info: "empty";
    callback request-open-file();
    VerticalBox {
        Text {
            text: "Files \{root.info}";
        }

        HorizontalLayout {
            padding: 10px;
            height: 12%;
            spacing: 10px;
            Rectangle {
                border-width: 2px;
                border-color: black;
                border-radius: 12px;
                Button {
                    width: 100%;
                    height: 100%;
                    text: "Refresh";
                    clicked => {
                        //todo
                    }
                }
            }

            Rectangle {
                border-width: 2px;
                border-color: black;
                border-radius: 12px;
                Button {
                    width: 100%;
                    height: 100%;
                    text: "Get File";
                    clicked => {
                        root.request-open-file();
                    }
                }
            }
        }
    }
}
